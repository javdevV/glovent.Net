@model IEnumerable<gloventApp.GUI.Models.galerieModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
 <div class="breadcrumb">
@using (Html.BeginForm())
{
   
    <table>
        <tr>
            <td>
                <div>
                    Search by name: @Html.TextBox("searching")
                </div>
            </td>
            <td> <input type="submit" value="Search" id="submit" class="btn btn-block btn-primary"/></td>

            <td>
                <div>
                    Search by name event: @Html.TextBox("serchingevent")
                </div>
            </td>
            <td> <input type="submit" value="Search" id="submit" class="btn btn-block btn-primary" />
            <td>
                <div>
                    Search by name categoey event: @Html.TextBox("serchingcat")
                </div>
            </td>
            <td> <input type="submit" value="Search" id="submit" class="btn btn-block btn-primary" /> </td>
        </tr>
    </table>
}
</div>

<div class="box">
    <div class="box-header">
        <h3 class="box-title">Hover Data Table</h3>
    </div>
    <!-- /.box-header -->
    <div class="box-body">
        <table id="example2" class="table table-bordered table-hover">
            <thead>
            <tbody>
                <tr>
                    <th>@Html.DisplayNameFor(model => model.name)</th>
                    <th>@Html.DisplayNameFor(model => model.evente.nameEvent)</th>
                    <th>Action</th>
                </tr>
                
            </tbody>
@foreach (var item in Model)
{
                <tr>
                    <td>@Html.DisplayFor(modelItem => item.name)</td>
                    <td>@Html.DisplayFor(modelItem => item.evente.nameEvent)</td>
                    <td>
                        @Html.ActionLink("Details", "Details", new { id = item.id }) |
                        @Html.ActionLink("Delete", "Delete", new { id = item.id })

                    </td>
                </tr>
}
        </table>
    </div>
</div>
<table>
    <tr>
        <td>
            <button type="button" class="btn btn-block btn-primary">@Html.ActionLink("UpdateVisibilite", "UpdateVisibilite")</button>
        </td>
       
         <td>
            <button type="button" class="btn btn-block btn-primary"> @Html.ActionLink("DeleteInvisible", "DeleteInvisible")</button>
        </td>
        <td>
            <button type="button" class="btn btn-block btn-primary"> @Html.ActionLink("GeneratePDF", "PrintInvoice")</button>
        </td>
    </tr>
    
    </table>
  
   
